# name: FTP Deploy to Hostinger

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: FTP Deploy
#         uses: SamKirkland/FTP-Deploy-Action@v4.3.4
#         with:
#           server: ${{ secrets.FTP_HOST }}
#           username: ${{ secrets.FTP_USERNAME }}
#           password: ${{ secrets.FTP_PASSWORD }}
#           port: ${{ secrets.FTP_PORT }}
#           server-dir: ${{ secrets.FTP_TARGET }}
#           local-dir: ./
#           exclude: |
#             **/vendor/**
#             **/node_modules/**
#             **/storage/**
#             **/.git/**
#             **/.github/**
#             **/.env


# ```
name: 🚀 Auto Deploy Laravel to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v3

      # 1️⃣ Upload Laravel backend (excluding public and vendor)
      - name: 🚚 Upload Laravel App to /laravel/
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: ${{ secrets.FTP_PORT }}
          server-dir: /laravel/
          local-dir: ./
          exclude: |
            public/**
            node_modules/**
            vendor/**
            .git/**
            .github/**
            tests/**
            README.md
            vite.config.js
            package.json
            package-lock.json
            .ftp-deploy-sync-state.json
          dangerous-clean-slate: false
          log-level: verbose

      # 2️⃣ Upload public folder to web root
      - name: 🌐 Upload public/ to /public_html/
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: ${{ secrets.FTP_PORT }}
          server-dir: /public_html/
          local-dir: ./public/
          exclude: |
            index.php   # we keep your custom Hostinger index.php
            .htaccess   # or remove this if you want to deploy your own .htaccess
          dangerous-clean-slate: false
          log-level: verbose
