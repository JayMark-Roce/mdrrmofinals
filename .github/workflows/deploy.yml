# name: FTP Deploy to Hostinger

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: FTP Deploy
#         uses: SamKirkland/FTP-Deploy-Action@v4.3.4
#         with:
#           server: ${{ secrets.FTP_HOST }}
#           username: ${{ secrets.FTP_USERNAME }}
#           password: ${{ secrets.FTP_PASSWORD }}
#           port: ${{ secrets.FTP_PORT }}
#           server-dir: ${{ secrets.FTP_TARGET }}
#           local-dir: ./
#           exclude: |
#             **/vendor/**
#             **/node_modules/**
#             **/storage/**
#             **/.git/**
#             **/.github/**
#             **/.env


# ```
name: 🚀 Auto Deploy to Hostinger

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v3

      # ✅ Deploy Laravel app EXCEPT /public to public_html/BayaniOnesys_v2/
      - name: 🚚 Deploy Laravel App (excluding public/) to /public_html/laravel/
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /public_html/laravel/
          local-dir: ./
          exclude: |
            public/**
            vendor/**
            .git/**
            .git*
            node_modules/**
            **/.env
            .env
            .tests/**
            README.md
            vite.config.js
            .editorconfig
            .ftp-deploy-sync-state.json
            app/Services/**
          dangerous-clean-slate: false
          log-level: verbose

      # ✅ Deploy only /public to /public_html/
      - name: 🌐 Deploy public files to /public_html/
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /
          local-dir: public/
          exclude: |
            favicon.ico
            index.html
          dangerous-clean-slate: false